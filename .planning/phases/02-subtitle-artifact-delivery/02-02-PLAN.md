---
phase: 02-subtitle-artifact-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/yt_subs/domain/models.py
  - src/yt_subs/infrastructure/yt_dlp_adapter.py
  - src/yt_subs/services/subtitles.py
  - tests/test_subtitle_downloader.py
  - tests/test_subtitle_service.py
autonomous: true
requirements: [SUBT-01, SUBT-03, SUBT-04, SUBT-05, EXPT-01, EXPT-02, EXPT-03, EXPT-05, META-01]
must_haves:
  truths:
    - "User can download subtitles for a single YouTube video without downloading video media."
    - "User can request one or more languages in one run."
    - "Unavailable requested languages are reported as unavailable, not as generic failures."
    - "Saved metadata identifies manual vs automatic tracks and links converted artifacts to source VTT files."
  artifacts:
    - path: "src/yt_subs/infrastructure/yt_dlp_adapter.py"
      provides: "yt-dlp subtitle-only downloader adapter"
      contains: "class YtDlpSubtitleDownloader"
    - path: "src/yt_subs/services/subtitles.py"
      provides: "single-video subtitle download orchestration and metadata persistence"
      exports: ["download_subtitles"]
    - path: "tests/test_subtitle_service.py"
      provides: "No-network service tests for language selection, missing language handling, and metadata JSON"
  key_links:
    - from: "src/yt_subs/services/subtitles.py"
      to: "src/yt_subs/services/subtitle_conversion.py"
      via: "convert downloaded source VTT into requested formats"
      pattern: "convert_vtt_artifacts"
    - from: "src/yt_subs/services/subtitles.py"
      to: "metadata/item.json"
      via: "Path.write_text with model_dump(mode='json')"
      pattern: "metadata_path.*write_text"
    - from: "src/yt_subs/infrastructure/yt_dlp_adapter.py"
      to: "yt_dlp.YoutubeDL"
      via: "skip_download subtitle-only options"
      pattern: "skip_download.*True"
---

<objective>
Implement the single-video subtitle download service that turns a YouTube URL plus language/format options into durable subtitle artifacts and metadata.

Purpose: This is the core Phase 2 user value: actual subtitle files, multi-language requests, unavailable-language reporting, manual/automatic distinction, and per-video metadata.
Output: A yt-dlp subtitle adapter plus service orchestration that writes subtitle artifacts under Phase 1 deterministic output identities.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md
@.planning/phases/01-inspectable-intake-job-setup/01-03-SUMMARY.md
@src/yt_subs/infrastructure/yt_dlp_adapter.py
@src/yt_subs/services/inspect.py
@src/yt_subs/services/subtitle_conversion.py
@src/yt_subs/domain/models.py

<interfaces>
Phase 1 adapter pattern:
```python
class YtDlpInspector:
    def __init__(self, ydl_options: dict[str, Any] | None = None) -> None: ...
    def inspect(self, url: str) -> list[InspectItem]:
        with YoutubeDL(self.ydl_options) as ydl:
            raw_info = ydl.extract_info(url, download=False)
```

Phase 1 service composition pattern:
```python
def inspect_target(url: str, options: JobOptions, *, inspector: Inspector | None = None) -> InspectReport:
    target = parse_target_url(url)
    items = inspector.inspect(str(target.url))
    identity = build_output_identity(item, options)
```

Plan 02-01 creates:
```python
def convert_vtt_artifacts(source_vtt: Path, subtitles_dir: Path, language_code: str, kind: SubtitleKind, requested_formats: list[SubtitleFormat]) -> list[SubtitleArtifact]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add yt-dlp subtitle-only downloader adapter</name>
  <files>src/yt_subs/infrastructure/yt_dlp_adapter.py, tests/test_subtitle_downloader.py</files>
  <read_first>src/yt_subs/infrastructure/yt_dlp_adapter.py, tests/test_inspect_service.py, src/yt_subs/domain/models.py, .planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Adapter initializes `YoutubeDL` with `skip_download=True`, `writesubtitles=True`, `writeautomaticsub=True`, requested `subtitleslangs`, and `subtitlesformat="vtt"`.
    - Test 2: Adapter calls `download([url])`, not `extract_info(..., download=True)`.
    - Test 3: Adapter returns discovered `.vtt` subtitle source paths from the planned subtitles directory.
  </behavior>
  <action>In `src/yt_subs/infrastructure/yt_dlp_adapter.py`, add `class YtDlpSubtitleDownloader` without changing `YtDlpInspector`. Constructor signature: `__init__(self, ydl_options: dict[str, Any] | None = None)`. Add method `download_subtitles(self, url: str, subtitles_dir: Path, languages: list[str], include_automatic: bool = True) -&gt; list[Path]`. It must create `subtitles_dir`, configure `YoutubeDL` with `ignoreconfig=True`, `quiet=True`, `skip_download=True`, `writesubtitles=True`, `writeautomaticsub=include_automatic`, `subtitleslangs=languages`, `subtitlesformat="vtt"`, and `outtmpl={"subtitle": str(subtitles_dir / "%(id)s.%(language)s.%(ext)s"), "default": str(subtitles_dir / "%(id)s.%(ext)s")}`. After `ydl.download([url])`, return `sorted(subtitles_dir.glob("*.vtt"))`. Keep this as infrastructure only; do not select languages or write metadata here.</action>
  <verify>
    <automated>uv run pytest tests/test_subtitle_downloader.py tests/test_inspect_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/yt_subs/infrastructure/yt_dlp_adapter.py` contains `class YtDlpSubtitleDownloader`.
    - `src/yt_subs/infrastructure/yt_dlp_adapter.py` contains `'skip_download': True` or `"skip_download": True`.
    - `src/yt_subs/infrastructure/yt_dlp_adapter.py` contains `'writesubtitles': True` or `"writesubtitles": True`.
    - `src/yt_subs/infrastructure/yt_dlp_adapter.py` contains `subtitlesformat` and `vtt`.
    - `tests/test_subtitle_downloader.py` asserts `download_calls == [[url]]` or equivalent list-call proof.
  </acceptance_criteria>
  <done>Adapter can be unit-tested without network and is configured for subtitle-only VTT source downloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate single-video subtitle artifacts and metadata</name>
  <files>src/yt_subs/domain/models.py, src/yt_subs/services/subtitles.py, tests/test_subtitle_service.py</files>
  <read_first>src/yt_subs/domain/models.py, src/yt_subs/services/inspect.py, src/yt_subs/domain/policies.py, src/yt_subs/services/subtitle_conversion.py, tests/test_inspect_service.py, .planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `download_subtitles` with languages `["en", "ja"]` returns artifacts for both languages when source VTT files exist.
    - Test 2: requested language `fr` absent from inspected tracks appears in `missing_subtitles` with reason `unavailable`.
    - Test 3: metadata JSON is written to `identity.metadata_path` and contains `video_id`, `requested_languages`, `artifacts`, and `missing_languages`.
    - Test 4: manual/automatic kind is preserved in artifact and metadata records.
  </behavior>
  <action>Add result/metadata contracts to `src/yt_subs/domain/models.py`: `VideoMetadata(video_id: str, title: str | None, webpage_url: AnyUrl, requested_languages: list[str], requested_formats: list[SubtitleFormat], include_automatic: bool, artifacts: list[SubtitleArtifact], missing_languages: list[MissingSubtitle])` and `SubtitleDownloadResult(item: InspectItem, identity: OutputIdentity, artifacts: list[SubtitleArtifact], missing_subtitles: list[MissingSubtitle], metadata_path: Path)`. Create `src/yt_subs/services/subtitles.py` exporting `download_subtitles(url: str, options: SubtitleDownloadOptions, *, inspector: Inspector | None = None, downloader: SubtitleDownloader | None = None) -&gt; SubtitleDownloadResult`. Use `parse_target_url`, inspect exactly one item, build/plan output identity, choose requested languages from `options.languages` or all available inspected languages, prefer manual track when both manual and automatic exist for a language, include automatic tracks only when `options.include_automatic` is true, mark unavailable requested languages as `MissingSubtitle(reason="unavailable")`, call downloader once with available requested languages and `identity.subtitles_dir`, convert each returned VTT with `convert_vtt_artifacts`, and write normalized JSON metadata to `identity.metadata_path` using `json.dumps(metadata.model_dump(mode="json"), ensure_ascii=False, indent=2)`. Do not implement playlist batch continuation, job manifest, structured run logs, or retries in this plan.</action>
  <verify>
    <automated>uv run pytest tests/test_subtitle_service.py tests/test_subtitle_conversion.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/yt_subs/services/subtitles.py` contains `def download_subtitles(`.
    - `src/yt_subs/services/subtitles.py` contains `MissingSubtitle`.
    - `src/yt_subs/services/subtitles.py` contains `convert_vtt_artifacts`.
    - `src/yt_subs/services/subtitles.py` contains `model_dump(mode="json")`.
    - `src/yt_subs/domain/models.py` contains `class VideoMetadata`.
    - `src/yt_subs/domain/models.py` contains `class SubtitleDownloadResult`.
    - `tests/test_subtitle_service.py` asserts metadata file exists at `metadata_path`.
  </acceptance_criteria>
  <done>Single-video subtitle service writes converted artifacts plus normalized metadata and distinguishes missing language outcomes.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_subtitle_downloader.py tests/test_subtitle_service.py tests/test_subtitle_conversion.py -x` and `uv run ruff check src tests`.
</verification>

<success_criteria>
- Subtitle download path uses yt-dlp with `skip_download=True`.
- Multi-language options produce per-language artifacts when available.
- Missing requested languages are returned as structured unavailable outcomes.
- `metadata/item.json` is persisted next to subtitle artifacts and includes provenance.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subtitle-artifact-delivery/02-02-SUMMARY.md`.
</output>
