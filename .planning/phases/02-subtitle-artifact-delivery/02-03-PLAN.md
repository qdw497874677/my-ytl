---
phase: 02-subtitle-artifact-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/yt_subs/interfaces/cli.py
  - src/yt_subs/interfaces/rendering.py
  - tests/test_cli_download.py
  - README.md
autonomous: true
requirements: [SUBT-01, SUBT-03, SUBT-04, SUBT-05, EXPT-01, EXPT-02, EXPT-03, EXPT-05, META-01]
must_haves:
  truths:
    - "User can run a documented CLI command to download subtitles for a video."
    - "User can pass multiple `--language` and `--format` options in one command."
    - "CLI output shows saved artifacts and unavailable requested languages."
    - "README documents subtitle download scope and artifact layout."
  artifacts:
    - path: "src/yt_subs/interfaces/cli.py"
      provides: "Typer download command for Phase 2 subtitle artifacts"
      contains: "def download"
    - path: "src/yt_subs/interfaces/rendering.py"
      provides: "Rich rendering for subtitle artifact result summaries"
      contains: "render_subtitle_download_result"
    - path: "README.md"
      provides: "User-facing subtitle download examples"
      contains: "Download Subtitles"
  key_links:
    - from: "src/yt_subs/interfaces/cli.py"
      to: "src/yt_subs/services/subtitles.py"
      via: "CLI delegates to download_subtitles service"
      pattern: "download_subtitles"
    - from: "src/yt_subs/interfaces/rendering.py"
      to: "SubtitleDownloadResult"
      via: "renders artifacts and missing subtitles from normalized result"
      pattern: "missing_subtitles"
---

<objective>
Expose subtitle artifact delivery through the CLI and documentation while keeping the command handler thin and service-backed.

Purpose: Users need a concrete command to request languages/formats and see where artifacts and metadata were saved.
Output: `yt-subdl download` command, Rich result rendering, CLI tests, and README examples for Phase 2.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md
@.planning/phases/01-inspectable-intake-job-setup/01-04-SUMMARY.md
@src/yt_subs/interfaces/cli.py
@src/yt_subs/interfaces/rendering.py
@src/yt_subs/services/subtitles.py
@README.md

<interfaces>
Existing CLI pattern:
```python
@app.command(help="Preview a YouTube video or playlist before download.")
def inspect(url: str, output_dir: str = typer.Option("downloads", "--output-dir", "-o")) -> None:
    report = inspect_target(url, JobOptions(output_dir=output_dir))
    render_inspect_result(report, console)
```

Plan 02-02 creates:
```python
def download_subtitles(url: str, options: SubtitleDownloadOptions, *, inspector: Inspector | None = None, downloader: SubtitleDownloader | None = None) -> SubtitleDownloadResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subtitle download CLI command and renderer</name>
  <files>src/yt_subs/interfaces/cli.py, src/yt_subs/interfaces/rendering.py, tests/test_cli_download.py</files>
  <read_first>src/yt_subs/interfaces/cli.py, src/yt_subs/interfaces/rendering.py, tests/test_cli_inspect.py, src/yt_subs/services/subtitles.py, src/yt_subs/domain/models.py</read_first>
  <behavior>
    - Test 1: `yt-subdl download URL --language en --language ja --format vtt --format txt --output-dir TMP` calls service with languages `["en", "ja"]`, formats `["vtt", "txt"]`, and output dir `TMP`.
    - Test 2: command output includes artifact paths and `metadata/item.json` path from a fake `SubtitleDownloadResult`.
    - Test 3: fake missing language `fr` appears in output with `unavailable`.
    - Test 4: `yt-subdl download --help` includes `--language`, `--format`, and examples.
  </behavior>
  <action>Update `src/yt_subs/interfaces/cli.py` to import `SubtitleDownloadOptions`, `SubtitleFormat`, `download_subtitles`, and `render_subtitle_download_result`. Add Typer command `download` with URL argument, repeatable option `--language`/`-l` as `list[str]`, repeatable option `--format`/`-f` as `list[str]` defaulting to `["vtt"]`, `--output-dir`/`-o` defaulting to `downloads`, and `--include-automatic/--manual-only` boolean defaulting true. Validate formats by constructing `SubtitleDownloadOptions`; on Pydantic `ValidationError`, print `Invalid download options: {exc}` and exit code 2. Delegate to `download_subtitles` and render the result. Update `src/yt_subs/interfaces/rendering.py` with `render_subtitle_download_result(result: SubtitleDownloadResult, console: Console) -&gt; None` that prints a table titled `Subtitle artifacts` with columns `Language`, `Kind`, `Format`, `Path`, `Source`, and a second table or lines for missing subtitles containing language and reason. Keep Rich usage in interfaces only.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_download.py tests/test_cli_inspect.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/yt_subs/interfaces/cli.py` contains `@app.command` immediately above or near `def download(`.
    - `src/yt_subs/interfaces/cli.py` contains `--language`.
    - `src/yt_subs/interfaces/cli.py` contains `--format`.
    - `src/yt_subs/interfaces/cli.py` contains `download_subtitles`.
    - `src/yt_subs/interfaces/rendering.py` contains `def render_subtitle_download_result`.
    - `tests/test_cli_download.py` asserts `fr` and `unavailable` appear in CLI output.
  </acceptance_criteria>
  <done>CLI exposes service-backed subtitle download with language/format options and renders artifacts plus missing-language outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 2 subtitle artifact usage</name>
  <files>README.md</files>
  <read_first>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update README from Phase 1-only wording to include Phase 2 subtitle artifact delivery. Add section `## Download Subtitles` with command example `uv run yt-subdl download "https://www.youtube.com/watch?v=VIDEOID" --language en --language ja --format vtt --format srt --format txt --output-dir downloads`. Document artifact layout exactly as `downloads/items/{video_id[-title]}/subtitles/`, `downloads/items/{video_id[-title]}/metadata/item.json`, and `downloads/items/{video_id[-title]}/logs/` reserved for later. State that VTT is the source subtitle artifact, SRT/TXT are converted exports, metadata links every converted file to its source track, unavailable requested languages are reported in CLI output and metadata, and playlist reruns/manifests/video downloads remain later phases.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_download.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Download Subtitles`.
    - `README.md` contains `--language en --language ja`.
    - `README.md` contains `--format vtt --format srt --format txt`.
    - `README.md` contains `metadata/item.json`.
    - `README.md` contains `VTT is the source subtitle artifact`.
    - `README.md` contains `playlist reruns` in the later-phase boundary text.
  </acceptance_criteria>
  <done>README tells users how to run subtitle downloads and where to find subtitles, metadata, and missing-language information.</done>
</task>

</tasks>

<verification>
Run `uv run pytest -q --tb=short`, `uv run ruff check src tests`, and `uv run python -m typer yt_subs.interfaces.cli run download --help`.
</verification>

<success_criteria>
- `yt-subdl download` accepts URL, output directory, repeated language options, repeated format options, and manual-only/automatic inclusion flag.
- CLI output surfaces artifact paths, metadata path, manual/automatic kind, and unavailable requested languages.
- README documents Phase 2 download usage and keeps Phase 3/video/API capabilities out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subtitle-artifact-delivery/02-03-SUMMARY.md`.
</output>
