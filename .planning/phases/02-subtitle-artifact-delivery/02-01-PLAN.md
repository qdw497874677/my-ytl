---
phase: 02-subtitle-artifact-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/yt_subs/domain/models.py
  - src/yt_subs/services/subtitle_conversion.py
  - tests/test_subtitle_contracts.py
  - tests/test_subtitle_conversion.py
autonomous: true
requirements: [EXPT-01, EXPT-02, EXPT-03, EXPT-05]
must_haves:
  truths:
    - "User can request subtitle artifact formats as explicit VTT, SRT, and TXT options."
    - "Converted SRT and TXT artifacts can be traced back to a source VTT subtitle track."
  artifacts:
    - path: "src/yt_subs/domain/models.py"
      provides: "Pydantic subtitle artifact, format, missing-language, and provenance contracts"
      contains: "class SubtitleArtifact"
    - path: "src/yt_subs/services/subtitle_conversion.py"
      provides: "Deterministic VTT-to-SRT and VTT-to-TXT conversion helpers"
      exports: ["convert_vtt_artifacts"]
    - path: "pyproject.toml"
      provides: "Runtime webvtt-py dependency for subtitle parsing/conversion"
      contains: "webvtt-py>=0.5.1,<0.6.0"
  key_links:
    - from: "src/yt_subs/services/subtitle_conversion.py"
      to: "webvtt-py"
      via: "webvtt.read for source VTT parsing"
      pattern: "webvtt\.read"
    - from: "src/yt_subs/services/subtitle_conversion.py"
      to: "src/yt_subs/domain/models.py"
      via: "returns SubtitleArtifact with SubtitleProvenance"
      pattern: "SubtitleProvenance"
---

<objective>
Create the subtitle artifact contracts and deterministic conversion core needed before actual downloads are wired.

Purpose: Phase 2 requires VTT/SRT/TXT exports and provenance; these contracts prevent the downloader, service, and CLI from inventing incompatible artifact shapes.
Output: API-ready Pydantic models plus tested VTT-to-SRT/TXT conversion helpers using `webvtt-py`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md
@.planning/phases/01-inspectable-intake-job-setup/01-01-SUMMARY.md
@src/yt_subs/domain/models.py
@src/yt_subs/domain/policies.py
@tests/test_domain_contracts.py

<interfaces>
From `src/yt_subs/domain/models.py` today:
```python
SubtitleKind = Literal["manual", "automatic"]

class SubtitleTrack(BaseModel):
    language_code: str = Field(min_length=1)
    language_name: str | None = None
    kind: SubtitleKind
    source_format: str | None = None

class JobOptions(BaseModel):
    output_dir: Path = Path("downloads")
    languages: list[str] = Field(default_factory=list)
    include_automatic: bool = True

class OutputIdentity(BaseModel):
    video_id: str
    item_key: str
    metadata_path: Path
    subtitles_dir: Path
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subtitle artifact domain contracts and conversion dependency</name>
  <files>pyproject.toml, uv.lock, src/yt_subs/domain/models.py, tests/test_subtitle_contracts.py</files>
  <read_first>pyproject.toml, src/yt_subs/domain/models.py, tests/test_domain_contracts.py, .planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `SubtitleDownloadOptions(output_dir=tmp_path, languages=["en", "ja"], formats=["vtt", "srt", "txt"])` preserves languages and formats.
    - Test 2: `SubtitleArtifact` requires language code, kind, output format, output path, and provenance source path/format.
    - Test 3: `MissingSubtitle(language_code="fr", reason="unavailable")` serializes to JSON-safe data.
  </behavior>
  <action>Add `webvtt-py>=0.5.1,<0.6.0` to `[project].dependencies` and refresh `uv.lock` with `uv lock`. Extend `src/yt_subs/domain/models.py` with concrete API-ready Pydantic contracts: `SubtitleFormat = Literal["vtt", "srt", "txt"]`; `SubtitleProvenance(source_path: Path, source_format: SubtitleFormat, source_language_code: str, source_kind: SubtitleKind)`; `SubtitleArtifact(language_code: str, kind: SubtitleKind, format: SubtitleFormat, path: Path, provenance: SubtitleProvenance)`; `MissingSubtitle(language_code: str, reason: Literal["unavailable"], detail: str | None = None)`; `SubtitleDownloadOptions(output_dir: Path = Path("downloads"), languages: list[str], formats: list[SubtitleFormat] = ["vtt"], include_automatic: bool = True)`. Keep domain free of Typer, Rich, yt-dlp, and webvtt imports. Do not remove existing Phase 1 models.</action>
  <verify>
    <automated>uv run pytest tests/test_subtitle_contracts.py tests/test_domain_contracts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `webvtt-py&gt;=0.5.1,&lt;0.6.0`.
    - `src/yt_subs/domain/models.py` contains `SubtitleFormat = Literal["vtt", "srt", "txt"]`.
    - `src/yt_subs/domain/models.py` contains `class SubtitleProvenance`.
    - `src/yt_subs/domain/models.py` contains `class SubtitleArtifact`.
    - `src/yt_subs/domain/models.py` contains `class MissingSubtitle`.
    - `src/yt_subs/domain/models.py` contains `class SubtitleDownloadOptions`.
  </acceptance_criteria>
  <done>Subtitle format/provenance contracts exist, lockfile is refreshed, and existing domain tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VTT to SRT/TXT conversion with provenance</name>
  <files>src/yt_subs/services/subtitle_conversion.py, tests/test_subtitle_conversion.py</files>
  <read_first>src/yt_subs/domain/models.py, src/yt_subs/services/__init__.py, .planning/phases/02-subtitle-artifact-delivery/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Given a tiny VTT file and requested formats `["vtt", "srt", "txt"]`, conversion returns three `SubtitleArtifact` entries.
    - Test 2: SRT output contains numbered captions and `00:00:00,000 --&gt; 00:00:01,000` comma timestamps.
    - Test 3: TXT output contains cleaned caption text without `WEBVTT`, timestamps, blank duplicate lines, or cue metadata.
    - Test 4: All artifacts include provenance pointing to the source VTT path and source format `vtt`.
  </behavior>
  <action>Create `src/yt_subs/services/subtitle_conversion.py` exporting `convert_vtt_artifacts(source_vtt: Path, subtitles_dir: Path, language_code: str, kind: SubtitleKind, requested_formats: list[SubtitleFormat]) -&gt; list[SubtitleArtifact]`. The function must create `subtitles_dir`, keep/copy the source VTT as `{language_code}.{kind}.vtt` when `vtt` is requested, generate `{language_code}.{kind}.srt` via `webvtt.read(source_vtt)` and `vtt.write(handle, format="srt")`, and generate `{language_code}.{kind}.txt` by iterating captions from `webvtt.read(source_vtt)`, using `caption.text`, stripping whitespace, dropping empty lines and consecutive duplicates, and writing UTF-8 text with one caption line per output line. Every returned artifact must use `SubtitleProvenance(source_path=source_vtt, source_format="vtt", source_language_code=language_code, source_kind=kind)`. Do not shell out to ffmpeg or use regex-only conversion.</action>
  <verify>
    <automated>uv run pytest tests/test_subtitle_conversion.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/yt_subs/services/subtitle_conversion.py` contains `def convert_vtt_artifacts(`.
    - `src/yt_subs/services/subtitle_conversion.py` contains `webvtt.read`.
    - `src/yt_subs/services/subtitle_conversion.py` contains `format="srt"`.
    - `tests/test_subtitle_conversion.py` contains `WEBVTT` fixture content.
    - `tests/test_subtitle_conversion.py` asserts `.txt` output text excludes timestamps.
  </acceptance_criteria>
  <done>Conversion helper produces deterministic VTT/SRT/TXT artifacts with source-track provenance and passing unit tests.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_subtitle_contracts.py tests/test_subtitle_conversion.py tests/test_domain_contracts.py -x` and `uv run ruff check src tests`.
</verification>

<success_criteria>
- Subtitle formats are typed as `vtt`, `srt`, and `txt`.
- Conversion outputs are deterministic under an item subtitles directory.
- Every converted artifact records source VTT provenance.
- Existing Phase 1 domain contracts remain compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-subtitle-artifact-delivery/02-01-SUMMARY.md`.
</output>
