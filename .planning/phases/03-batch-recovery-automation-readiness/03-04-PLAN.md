---
phase: 03-batch-recovery-automation-readiness
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/yt_subs/interfaces/cli.py
  - src/yt_subs/interfaces/rendering.py
  - tests/test_cli_batch.py
  - README.md
autonomous: true
requirements:
  - SUBT-02
  - META-02
  - META-03
  - RELY-01
  - RELY-02
  - RELY-03
  - RELY-04
  - CLIX-02
  - CLIX-03
must_haves:
  truths:
    - "User can start a playlist batch job from the CLI with familiar language/format/output options."
    - "User can rerun failed items from a saved manifest from the CLI."
    - "User can see progress and final success/failure counts in terminal output."
    - "User can request machine-readable JSON summary output for scripting."
  artifacts:
    - path: "src/yt_subs/interfaces/cli.py"
      provides: "batch and rerun CLI commands"
      contains: "def batch"
    - path: "src/yt_subs/interfaces/rendering.py"
      provides: "Rich progress/final summary rendering for batch results"
      contains: "render_batch_run_summary"
    - path: "tests/test_cli_batch.py"
      provides: "CLI option passing and JSON summary behavior coverage"
    - path: "README.md"
      provides: "Phase 3 user documentation for batch, manifests, logs, reruns, and JSON summaries"
  key_links:
    - from: "src/yt_subs/interfaces/cli.py"
      to: "src/yt_subs/services/batch.py"
      via: "batch command delegates to run_batch_subtitle_job"
      pattern: "run_batch_subtitle_job"
    - from: "src/yt_subs/interfaces/cli.py"
      to: "src/yt_subs/services/rerun.py"
      via: "rerun command delegates to rerun_failed_items"
      pattern: "rerun_failed_items"
---

<objective>
Expose Phase 3 batch execution, failed-item reruns, progress summaries, and automation-friendly JSON output through the CLI and README.

Purpose: The resilient services become useful only when users can run them with documented commands and scripting-compatible outputs.
Output: CLI commands, Rich renderers, CLI tests, and README documentation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-subtitle-artifact-delivery/02-03-SUMMARY.md
@.planning/phases/03-batch-recovery-automation-readiness/03-02-SUMMARY.md
@.planning/phases/03-batch-recovery-automation-readiness/03-03-SUMMARY.md
@src/yt_subs/interfaces/cli.py
@src/yt_subs/interfaces/rendering.py
@README.md

<interfaces>
Existing CLI pattern:
```python
@app.command(...)
def download(url: str, language: list[str] | None = None, format: list[str] | None = None, output_dir: str = "downloads", include_automatic: bool = True) -> None:
    options = SubtitleDownloadOptions(...)
    result = download_subtitles(url, options)
    render_subtitle_download_result(result, console)
```
Follow this thin-handler pattern for batch and rerun. Do not put orchestration logic in `interfaces/cli.py`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add batch and rerun CLI commands with JSON summary option</name>
  <files>src/yt_subs/interfaces/cli.py, tests/test_cli_batch.py</files>
  <behavior>
    - `yt-subdl batch URL --language en --format vtt --format srt --output-dir downloads` delegates to the batch service with matching options.
    - `yt-subdl rerun MANIFEST_PATH` delegates to rerun_failed_items.
    - `--json-summary` prints the machine-readable summary JSON and does not require scraping Rich tables.
    - Validation errors exit 2; execution failures exit 1 with a concise error message.
  </behavior>
  <action>Add `batch` and `rerun` Typer commands to `src/yt_subs/interfaces/cli.py`. Reuse repeatable `--language` and `--format` semantics from the existing download command. Add `--json-summary/--no-json-summary` or `--json-summary` flag for automation per CLIX-03. Monkeypatch service calls in CLI tests; do not perform network access.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_batch.py::test_batch_command_passes_options_and_prints_json_summary -x</automated>
  </verify>
  <done>CLI tests prove command names, options, service delegation, and JSON summary output work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render human-readable batch progress and final summaries</name>
  <files>src/yt_subs/interfaces/rendering.py, tests/test_cli_batch.py</files>
  <behavior>
    - Renderer shows manifest path, log path, total counts, completed/skipped/no_subtitles/failed counts.
    - Renderer shows retryable vs permanent failure counts separately.
    - CLI progress callback emits readable item transition lines without raw Pydantic reprs.
  </behavior>
  <action>Add `render_batch_run_summary(summary, console)` and a small progress-event renderer if needed. Keep Rich output human-readable and normalized. The JSON summary path must remain separate and machine-readable. This implements CLIX-02 without compromising CLIX-03.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_batch.py -x</automated>
  </verify>
  <done>Human output includes progress/final counts while JSON mode returns parseable summary JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Document batch manifests, logs, reruns, and automation output</name>
  <files>README.md</files>
  <action>Update README with Phase 3 examples: playlist batch command, repeated language/format options, manifest/log/summary file locations, rerun command from manifest path, skip-completed behavior, retryable vs permanent failures, and `--json-summary` for scripts. Explicitly state video download and HTTP API remain future scope.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_batch.py -x</automated>
  </verify>
  <done>README contains executable command examples and explains all durable outputs required by Phase 3.</done>
</task>

</tasks>

<verification>
Run `uv run pytest -q --tb=short` and `uv run ruff check src tests`.
</verification>

<success_criteria>
- Users can run playlist batch and manifest rerun workflows from CLI.
- Terminal output gives progress and final counts.
- JSON summary output is parseable for scripts and future API compatibility.
- README documents durable artifacts and Phase 3 scope boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-batch-recovery-automation-readiness/03-04-SUMMARY.md`
</output>
