---
phase: 03-batch-recovery-automation-readiness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/yt_subs/services/batch.py
  - tests/test_batch_service.py
autonomous: true
requirements:
  - SUBT-02
  - META-02
  - META-03
  - RELY-01
  - RELY-03
  - RELY-04
  - CLIX-02
must_haves:
  truths:
    - "User can run a playlist subtitle job and one failed item does not stop later items."
    - "User gets a manifest and structured logs with per-item statuses, artifact paths, and errors."
    - "User can skip already completed artifacts without duplicating finished work."
    - "User can see execution progress as items start, complete, skip, or fail."
  artifacts:
    - path: "src/yt_subs/services/batch.py"
      provides: "Playlist-scale subtitle batch orchestration service"
      exports: ["run_batch_subtitle_job"]
    - path: "tests/test_batch_service.py"
      provides: "Batch continuation, skip, failure classification, manifest/log coverage"
  key_links:
    - from: "src/yt_subs/services/batch.py"
      to: "src/yt_subs/services/subtitles.py"
      via: "download_subtitles per inspected item"
      pattern: "download_subtitles"
    - from: "src/yt_subs/services/batch.py"
      to: "src/yt_subs/services/job_manifest.py"
      via: "write_manifest and append_run_log after every item state transition"
      pattern: "append_run_log"
---

<objective>
Implement resilient playlist-scale subtitle batch execution over the existing inspect and single-video download services.

Purpose: Users need playlist jobs that are durable and recoverable instead of all-or-nothing single-video loops.
Output: A tested batch service that expands playlist items, processes each independently, persists status/logs, skips completed artifacts, and returns a summary.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-batch-recovery-automation-readiness/03-01-SUMMARY.md
@src/yt_subs/services/inspect.py
@src/yt_subs/services/subtitles.py
@src/yt_subs/services/job_manifest.py

<interfaces>
Use these existing service boundaries:
```python
def inspect_target(url: str, options: JobOptions, *, inspector: Inspector | None = None) -> InspectReport

def download_subtitles(
    url: str,
    options: SubtitleDownloadOptions,
    *,
    inspector: InspectorProto | None = None,
    downloader: DownloaderProto | None = None,
) -> SubtitleDownloadResult
```
Use 03-01 contracts exactly as written for manifest records, run logs, failure records, and summaries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add batch service contracts and playlist item loop</name>
  <files>src/yt_subs/services/batch.py, tests/test_batch_service.py</files>
  <behavior>
    - Given an inspect report with three playlist items, the service attempts all three even when the middle item raises an exception.
    - Completed items include artifacts and metadata_path from SubtitleDownloadResult.
    - Failed exceptions are captured as failed_retryable with a FailureRecord unless explicitly classified permanent.
    - Manifest is written before the first item and after every item completion/failure so interruption leaves usable state.
  </behavior>
  <action>Create `run_batch_subtitle_job(url, options, *, inspector=None, downloader_factory=None, progress_callback=None)` in `src/yt_subs/services/batch.py`. Define a small BatchSubtitleOptions Pydantic model if needed, or reuse SubtitleDownloadOptions plus source_url. Internally call `inspect_target` once to expand playlist items, then call `download_subtitles` for each item URL using fakeable dependencies in tests. Since existing `download_subtitles` inspects one URL itself, build tests with injectable fake download callable/factory rather than live network. This implements SUBT-02, RELY-01, META-02, and META-03.</action>
  <verify>
    <automated>uv run pytest tests/test_batch_service.py::test_batch_continues_after_retryable_item_failure -x</automated>
  </verify>
  <done>A failing item records failed_retryable and subsequent items still complete in manifest and summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add skip-completed and progress event behavior</name>
  <files>src/yt_subs/services/batch.py, tests/test_batch_service.py</files>
  <behavior>
    - If an inspected item's metadata_path exists and requested artifacts already exist, the item is marked skipped and no download call is made.
    - no-subtitles outcomes are classified permanent/no_subtitles, not retryable crashes.
    - progress_callback receives item_started, item_completed, item_skipped, item_failed, and job_completed events.
    - JSONL logs contain matching durable events for progress and automation.
  </behavior>
  <action>Extend the batch service with skip checks based on output identity paths and requested formats/languages. Keep skip logic conservative: only skip when metadata exists and required artifact paths are present; otherwise redownload. Classify unavailable/no subtitle outcomes as no_subtitles or failed_permanent per 03-01 taxonomy. Invoke `progress_callback(event)` after durable log append so terminal progress never outruns persistence. This implements RELY-03, RELY-04, and CLIX-02.</action>
  <verify>
    <automated>uv run pytest tests/test_batch_service.py -x</automated>
  </verify>
  <done>Tests prove completed artifacts are skipped, no-subtitle results are permanent, and progress/log events reflect each item transition.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_batch_service.py tests/test_job_manifest.py -x` and `uv run ruff check src tests`.
</verification>

<success_criteria>
- Playlist jobs process all supported items independently.
- Manifest/log/summary are updated throughout execution.
- Retryable, permanent, no-subtitle, completed, and skipped outcomes are machine-readable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-batch-recovery-automation-readiness/03-02-SUMMARY.md`
</output>
