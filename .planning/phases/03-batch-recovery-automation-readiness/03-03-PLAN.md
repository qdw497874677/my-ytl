---
phase: 03-batch-recovery-automation-readiness
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/yt_subs/services/rerun.py
  - tests/test_rerun_service.py
autonomous: true
requirements:
  - META-02
  - RELY-02
  - RELY-03
  - RELY-04
must_haves:
  truths:
    - "User can rerun only failed retryable items from a previous manifest."
    - "User can avoid duplicating work for completed or skipped items during rerun."
    - "User can preserve permanent failure/no-subtitle records without repeatedly retrying them."
  artifacts:
    - path: "src/yt_subs/services/rerun.py"
      provides: "Manifest-driven failed-item rerun service"
      exports: ["rerun_failed_items"]
    - path: "tests/test_rerun_service.py"
      provides: "Rerun selection and manifest update coverage"
  key_links:
    - from: "src/yt_subs/services/rerun.py"
      to: "src/yt_subs/services/job_manifest.py"
      via: "read previous manifest and write updated rerun manifest/summary"
      pattern: "read_manifest"
    - from: "src/yt_subs/services/rerun.py"
      to: "src/yt_subs/services/batch.py"
      via: "reuse batch item execution/skip semantics or same contracts"
      pattern: "failed_retryable"
---

<objective>
Add manifest-driven rerun behavior for failed playlist items.

Purpose: A durable batch tool is only useful if failed work can be recovered without repeating successful downloads.
Output: A tested rerun service that selects retryable failures, leaves permanent outcomes alone, skips already completed artifacts, and writes updated manifest/log/summary records.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-batch-recovery-automation-readiness/03-01-SUMMARY.md
@.planning/phases/03-batch-recovery-automation-readiness/03-02-SUMMARY.md
@src/yt_subs/services/job_manifest.py
@src/yt_subs/services/batch.py

<interfaces>
Rerun must consume the 03-01 BatchJobManifest and status taxonomy. Only `failed_retryable` items are selected by default. Do not retry `completed`, `skipped`, `failed_permanent`, or `no_subtitles` records.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select retryable failures from manifest</name>
  <files>src/yt_subs/services/rerun.py, tests/test_rerun_service.py</files>
  <behavior>
    - Given a manifest with completed, skipped, failed_retryable, failed_permanent, and no_subtitles items, only failed_retryable records are selected.
    - If no retryable failures exist, rerun returns a summary with zero attempted items and still points to the source manifest.
    - Selection preserves original InspectItem, OutputIdentity, requested languages/formats/include_automatic, and attempts count.
  </behavior>
  <action>Create `src/yt_subs/services/rerun.py` with `rerun_failed_items(manifest_path, *, downloader_factory=None, progress_callback=None)`. Start by reading the manifest and selecting only retryable failures per RELY-02/RELY-03. Reuse 03-01 models; do not introduce a database or ad-hoc JSON schema.</action>
  <verify>
    <automated>uv run pytest tests/test_rerun_service.py::test_rerun_selects_only_retryable_failures -x</automated>
  </verify>
  <done>Retry candidate selection is deterministic and permanent/no-subtitle/completed items are never attempted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute rerun attempts and update durable records</name>
  <files>src/yt_subs/services/rerun.py, tests/test_rerun_service.py</files>
  <behavior>
    - Successful rerun changes item status to completed and increments attempts.
    - Already completed artifacts are marked skipped without calling the downloader.
    - Repeat failures remain failed_retryable or become failed_permanent based on failure classification.
    - Rerun appends JSONL events and writes an updated summary.
  </behavior>
  <action>Implement rerun execution using the same conservative skip and failure-classification semantics as `batch.py`. Either extract tiny shared helpers from `batch.py` if needed or import functions from it, but do not duplicate incompatible status logic. Persist manifest/log/summary after each rerun item. This implements RELY-02 and RELY-04.</action>
  <verify>
    <automated>uv run pytest tests/test_rerun_service.py tests/test_batch_service.py -x</automated>
  </verify>
  <done>Rerun updates only retryable failures, avoids duplicate work, and maintains manifest/log/summary consistency.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_rerun_service.py tests/test_batch_service.py tests/test_job_manifest.py -x` and `uv run ruff check src tests`.
</verification>

<success_criteria>
- Rerun is manifest-driven and requires no live playlist re-expansion to know which failed items to attempt.
- Completed/permanent/no-subtitle records are preserved.
- Updated manifest and summary remain valid inputs for future reruns or API use.
</success_criteria>

<output>
After completion, create `.planning/phases/03-batch-recovery-automation-readiness/03-03-SUMMARY.md`
</output>
