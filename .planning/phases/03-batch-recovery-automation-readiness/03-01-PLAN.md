---
phase: 03-batch-recovery-automation-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/yt_subs/domain/models.py
  - src/yt_subs/services/job_manifest.py
  - tests/test_job_manifest.py
autonomous: true
requirements:
  - META-02
  - META-03
  - RELY-03
  - CLIX-03
must_haves:
  truths:
    - "User can inspect a saved batch manifest and see requested inputs, expanded items, statuses, artifacts, and errors."
    - "User can distinguish retryable failures from permanent no-subtitle or unavailable-content outcomes in persisted records."
    - "Automation can parse a stable JSON summary without scraping Rich terminal output."
  artifacts:
    - path: "src/yt_subs/domain/models.py"
      provides: "Batch manifest, item status, failure classification, structured log event, and machine-readable summary contracts"
      contains: "BatchJobManifest"
    - path: "src/yt_subs/services/job_manifest.py"
      provides: "JSON persistence helpers for manifests, JSONL run logs, and summaries"
      exports: ["write_manifest", "read_manifest", "append_run_log", "write_run_summary"]
    - path: "tests/test_job_manifest.py"
      provides: "Contract and persistence regression coverage"
  key_links:
    - from: "src/yt_subs/services/job_manifest.py"
      to: "src/yt_subs/domain/models.py"
      via: "Pydantic model_dump(mode='json') and model_validate_json"
      pattern: "model_dump\(mode=\"json\"\)"
---

<objective>
Create the API-ready batch state contracts and filesystem persistence primitives used by playlist execution, reruns, logs, and machine-readable summaries.

Purpose: Phase 3 depends on durable run records; build the manifest/log/summary foundation before orchestration so downstream services do not invent incompatible JSON shapes.
Output: Domain contracts plus tested JSON/JSONL persistence helpers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-subtitle-artifact-delivery/02-02-SUMMARY.md
@src/yt_subs/domain/models.py
@src/yt_subs/services/subtitles.py

<interfaces>
Existing contracts to extend, not replace:
```python
class InspectItem(BaseModel):
    video_id: str
    title: str | None = None
    webpage_url: AnyUrl
    playlist_index: int | None = Field(default=None, ge=1)
    subtitles: list[SubtitleTrack] = Field(default_factory=list)

class OutputIdentity(BaseModel):
    video_id: str
    item_key: str
    bundle_dir: Path
    metadata_path: Path
    subtitles_dir: Path
    logs_dir: Path

class SubtitleDownloadResult(BaseModel):
    item: InspectItem
    identity: OutputIdentity
    artifacts: list[SubtitleArtifact]
    missing_subtitles: list[MissingSubtitle]
    metadata_path: Path
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add batch manifest and failure taxonomy contracts</name>
  <files>src/yt_subs/domain/models.py, tests/test_job_manifest.py</files>
  <behavior>
    - A BatchJobManifest records job_id, created_at ISO string, source_url, output_dir, requested languages/formats/include_automatic, manifest_path, log_path, summary_path, and item records.
    - Each BatchItemRecord records InspectItem, OutputIdentity, status, artifacts, missing_subtitles, error, attempts, started_at, completed_at.
    - Status values are exactly pending, running, completed, skipped, failed_retryable, failed_permanent, no_subtitles.
    - FailureRecord has category retryable or permanent plus code/message/detail; unavailable subtitles and unsupported content are permanent.
    - BatchRunSummary is JSON-safe and includes total/completed/skipped/failed_retryable/failed_permanent/no_subtitles counts plus manifest/log paths.
  </behavior>
  <action>Add Pydantic domain models and Literal aliases in `domain/models.py`. Keep models pure: no Typer, Rich, yt-dlp, or service imports. Prefer frozen models where existing domain contracts do. Use Path fields for persisted file paths and `Field(default_factory=list)` for collections. This implements META-02, RELY-03, and CLIX-03.</action>
  <verify>
    <automated>uv run pytest tests/test_job_manifest.py::test_batch_manifest_contracts_are_json_safe -x</automated>
  </verify>
  <done>Tests prove a manifest with completed, retryable, permanent, and no-subtitles item records serializes with `model_dump(mode="json")` and validates back into domain models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement manifest, JSONL log, and summary persistence helpers</name>
  <files>src/yt_subs/services/job_manifest.py, tests/test_job_manifest.py</files>
  <behavior>
    - write_manifest creates parent directories and writes deterministic pretty JSON.
    - read_manifest loads the saved JSON into BatchJobManifest.
    - append_run_log writes one JSON object per line with event name, item video_id if present, level, message, timestamp, and details.
    - write_run_summary computes counts from manifest item statuses and writes JSON for automation.
  </behavior>
  <action>Create `src/yt_subs/services/job_manifest.py` with persistence helpers. Use UTF-8, `ensure_ascii=False`, and Pydantic `model_dump(mode="json")`. Do not use structlog yet for persistence; this file owns durable job records while later orchestration may also emit terminal progress. This implements META-02, META-03, and CLIX-03.</action>
  <verify>
    <automated>uv run pytest tests/test_job_manifest.py -x</automated>
  </verify>
  <done>Manifest JSON, JSONL log entries, and summary JSON are persisted under a temp output directory and read back without type loss.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_job_manifest.py -x` and `uv run ruff check src tests`.
</verification>

<success_criteria>
- Batch manifest, failure taxonomy, durable run log, and machine-readable summary contracts exist.
- Persistence helpers are deterministic, JSON-safe, and independent of CLI rendering.
- Downstream batch and rerun services can import these contracts without circular dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/03-batch-recovery-automation-readiness/03-01-SUMMARY.md`
</output>
