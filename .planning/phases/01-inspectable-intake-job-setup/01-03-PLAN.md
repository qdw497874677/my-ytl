---
phase: 01-inspectable-intake-job-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/yt_subs/domain/url.py
  - src/yt_subs/infrastructure/yt_dlp_adapter.py
  - src/yt_subs/services/inspect.py
  - tests/test_url_intake.py
  - tests/test_inspect_service.py
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
  - INTK-04
  - EXPT-04
  - META-04
must_haves:
  truths:
    - "User can provide common YouTube video URL variants and receive a normalized target request."
    - "User can provide a playlist URL and the inspect service models it as per-video items rather than one opaque batch."
    - "Inspect service returns available subtitle languages for each inspected item without downloading subtitle artifacts."
    - "Each inspected item includes a stable output identity and planned paths based on video ID."
  artifacts:
    - path: "src/yt_subs/domain/url.py"
      provides: "YouTube URL intake classification and light normalization"
      exports: ["parse_target_url"]
    - path: "src/yt_subs/infrastructure/yt_dlp_adapter.py"
      provides: "Only direct yt-dlp metadata extraction boundary"
      exports: ["YtDlpInspector"]
    - path: "src/yt_subs/services/inspect.py"
      provides: "Application inspect use case returning normalized items/languages/path plans"
      exports: ["inspect_target"]
  key_links:
    - from: "src/yt_subs/services/inspect.py"
      to: "src/yt_subs/infrastructure/yt_dlp_adapter.py"
      via: "service calls adapter metadata-only inspection"
      pattern: "YtDlpInspector"
    - from: "src/yt_subs/services/inspect.py"
      to: "src/yt_subs/domain/policies.py"
      via: "service attaches deterministic output identity/path plans"
      pattern: "build_output_identity|plan_item_paths"
    - from: "src/yt_subs/infrastructure/yt_dlp_adapter.py"
      to: "yt_dlp.YoutubeDL"
      via: "single adapter boundary uses extract_info(..., download=False)"
      pattern: "extract_info\(.*download=False"
---

<objective>
Implement URL intake and the metadata-only inspection service over an isolated yt-dlp adapter.

Purpose: Phase 1 is inspect-first. This plan proves common video/playlist targets can be inspected and expanded into API-ready item models before any subtitle download pipeline exists.
Output: A tested inspect service that normalizes YouTube targets, calls yt-dlp only through one adapter, returns per-item subtitle availability, and attaches deterministic identity/path plans.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inspectable-intake-job-setup/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-inspectable-intake-job-setup/01-01-SUMMARY.md

<interfaces>
Use Plan 01 contracts:
```python
from yt_subs.domain.models import TargetRequest, InspectItem, SubtitleTrack, JobOptions
from yt_subs.domain.policies import build_output_identity, plan_item_paths
```

yt-dlp boundary requirements from research:
- Direct `YoutubeDL(...)`, `extract_info(...)`, and `sanitize_info(...)` usage belongs only in `infrastructure/yt_dlp_adapter.py`.
- Inspection must call metadata extraction with `download=False`.
- Playlist input must become per-video item records, not one monolithic status.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add common YouTube URL intake parsing</name>
  <files>src/yt_subs/domain/url.py, tests/test_url_intake.py</files>
  <behavior>
    - Test 1: standard `https://www.youtube.com/watch?v=VIDEOID` parses as a video target.
    - Test 2: short `https://youtu.be/VIDEOID` and Shorts `https://www.youtube.com/shorts/VIDEOID` parse without manual user normalization for INTK-03.
    - Test 3: URLs with `list=PLAYLISTID` parse as playlist-capable targets while preserving the original URL for yt-dlp.
    - Test 4: non-YouTube URLs raise a clear validation error before adapter execution.
  </behavior>
  <action>Write tests first, then implement light URL parsing/classification. Preserve the original URL for yt-dlp because yt-dlp remains the source of extraction truth; this layer only classifies common YouTube variants and catches obviously unsupported input early. Address INTK-01, INTK-02, and INTK-03. Do not add Invidious/Piped mirror support because that URL support range was deferred in CONTEXT.md.</action>
  <verify>
    <automated>uv run pytest tests/test_url_intake.py -x</automated>
  </verify>
  <done>Common watch/share/shorts/playlist YouTube variants are accepted without manual normalization, and unsupported domains fail clearly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement isolated yt-dlp metadata inspection adapter</name>
  <files>src/yt_subs/infrastructure/yt_dlp_adapter.py, tests/test_inspect_service.py</files>
  <behavior>
    - Test 1: adapter calls a monkeypatched `YoutubeDL.extract_info(url, download=False)` for metadata-only inspection.
    - Test 2: adapter normalizes a single-video info dict into one item with manual and automatic subtitle language records.
    - Test 3: adapter normalizes playlist `entries` into multiple per-video inspected item records.
  </behavior>
  <action>Write adapter tests with monkeypatched/fake yt-dlp objects so tests never hit the network. Implement `YtDlpInspector` as the only module that imports `yt_dlp`. Configure embedded yt-dlp to ignore ambient user/system config where supported (e.g. use app-owned options such as `ignoreconfig`/equivalent) to reduce config leakage per PITFALLS. Use `sanitize_info` when available before converting metadata to domain-safe structures. Do not download subtitles or media; inspection only.</action>
  <verify>
    <automated>uv run pytest tests/test_inspect_service.py -x</automated>
  </verify>
  <done>Adapter tests prove metadata-only extraction, playlist expansion, and subtitle-language normalization without network access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose inspect service with output identity planning</name>
  <files>src/yt_subs/services/inspect.py, tests/test_inspect_service.py</files>
  <behavior>
    - Test 1: `inspect_target(url, options)` returns inspected items with available subtitle tracks for INTK-04.
    - Test 2: every returned item includes an output identity/path plan that contains its video ID for EXPT-04 and META-04.
    - Test 3: playlist inspection preserves per-item order/index when supplied by the adapter.
  </behavior>
  <action>Write service tests using a fake inspector dependency, then implement `inspect_target`. The service should call URL parsing, delegate metadata work to `YtDlpInspector`, and attach Plan 01 identity/path plans to each item. Keep this as application service code; no Rich rendering, Typer parsing, or file writes here. This task completes INTK-04 at the service layer and keeps inspect as a first-class use case for D-02.</action>
  <verify>
    <automated>uv run pytest tests/test_url_intake.py tests/test_inspect_service.py -x</automated>
  </verify>
  <done>Inspect service returns per-video inspected items, subtitle availability, playlist expansion, and deterministic planned paths without invoking CLI or downloading artifacts.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_url_intake.py tests/test_inspect_service.py -x` and `uv run ruff check src tests`. Confirm `yt_dlp` is imported only in `src/yt_subs/infrastructure/yt_dlp_adapter.py`.
</verification>

<success_criteria>
- Common YouTube URL variants are accepted and classified.
- Metadata-only yt-dlp inspection is isolated behind one adapter.
- Playlist targets produce per-video item records with subtitle language availability.
- Inspect service attaches stable item identities and output paths that include video IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-inspectable-intake-job-setup/01-03-SUMMARY.md`
</output>
