---
phase: 01-inspectable-intake-job-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/yt_subs/infrastructure/__init__.py
  - src/yt_subs/infrastructure/preflight.py
  - src/yt_subs/services/__init__.py
  - src/yt_subs/services/preflight.py
  - src/yt_subs/interfaces/__init__.py
  - src/yt_subs/interfaces/cli.py
  - tests/test_preflight.py
  - tests/test_cli_preflight.py
autonomous: true
requirements:
  - RELY-05
  - CLIX-01
must_haves:
  truths:
    - "User can run a documented CLI subcommand to check whether downloader-related dependencies are available before starting a job."
    - "Preflight output distinguishes required Python package availability from external binary/runtime checks."
    - "The CLI entry remains a thin adapter over service code, not the owner of runtime-check logic."
  artifacts:
    - path: "src/yt_subs/infrastructure/preflight.py"
      provides: "Runtime dependency probes for yt-dlp, ffmpeg, ffprobe, and JS runtime candidates"
      exports: ["collect_preflight_checks"]
    - path: "src/yt_subs/services/preflight.py"
      provides: "Application service that summarizes runtime readiness"
      exports: ["run_preflight"]
    - path: "src/yt_subs/interfaces/cli.py"
      provides: "Typer root app and first-class preflight subcommand"
      contains: "@app.command"
  key_links:
    - from: "src/yt_subs/interfaces/cli.py"
      to: "src/yt_subs/services/preflight.py"
      via: "CLI calls service only"
      pattern: "run_preflight"
    - from: "src/yt_subs/services/preflight.py"
      to: "src/yt_subs/infrastructure/preflight.py"
      via: "service composes infrastructure probes"
      pattern: "collect_preflight_checks"
---

<objective>
Implement the runtime preflight workflow and the initial Typer CLI shell.

Purpose: Users need a safe way to verify their local downloader environment before inspect/download work. This also proves the CLI-first shape while preserving service boundaries for future API use.
Output: A `yt-subdl preflight` command backed by service/infrastructure code and tested without requiring real system binaries to be present.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inspectable-intake-job-setup/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-inspectable-intake-job-setup/01-01-SUMMARY.md

<interfaces>
From Plan 01 contracts, use rather than redefine:
```python
from yt_subs.domain.models import JobOptions
```

Preflight service contract to create:
```python
def run_preflight() -> PreflightReport: ...
```

CLI shape constraints from CONTEXT.md:
- D-01: single main CLI entry plus subcommands.
- D-03: temporary internal entry name is acceptable and replaceable.
- D-05: help text should be operational and example-driven.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build preflight probe and report service</name>
  <files>src/yt_subs/infrastructure/preflight.py, src/yt_subs/services/preflight.py, tests/test_preflight.py</files>
  <behavior>
    - Test 1: monkeypatched probes report installed `yt-dlp` version as available.
    - Test 2: missing `ffmpeg` or `ffprobe` appears as a failed check without crashing the report.
    - Test 3: at least one JS runtime candidate (`deno`, `node`, `bun`, or `quickjs`) can satisfy the JS runtime advisory check.
  </behavior>
  <action>Write tests first, then implement preflight checks. Use Python imports/version metadata for `yt-dlp`; use `shutil.which` plus safe version subprocess calls for `ffmpeg`, `ffprobe`, and JS runtime candidates. The service should return structured Pydantic/dataclass-like report objects with check name, status, version/path when available, severity (`required` vs `recommended`), and remediation hint. Address RELY-05. Do not fail the whole command because one optional/recommended dependency is missing; classify it in the report. Do not use print statements in infrastructure/service code.</action>
  <verify>
    <automated>uv run pytest tests/test_preflight.py -x</automated>
  </verify>
  <done>Preflight report is deterministic under monkeypatched probes and clearly records missing required/recommended downloader dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Typer root app and preflight command</name>
  <files>src/yt_subs/interfaces/__init__.py, src/yt_subs/interfaces/cli.py, tests/test_cli_preflight.py</files>
  <behavior>
    - Test 1: `CliRunner().invoke(app, ["--help"])` shows subcommands and operation-oriented examples per D-05.
    - Test 2: `CliRunner().invoke(app, ["preflight"])` calls the preflight service and exits 0 when required checks pass.
    - Test 3: the CLI command renders missing required checks and exits non-zero when the service reports required failures.
  </behavior>
  <action>Create a single Typer root app per D-01 and expose `preflight` as a subcommand. Keep the temporary script name from Plan 01 per D-03; do not introduce multiple executables. The CLI handler must call `services.preflight.run_preflight()` and render with Rich, but all probe logic stays below the CLI. Help text must include common examples and next-step guidance per D-05. Avoid adding `inspect` here; Plan 04 wires it after the inspect service exists.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_preflight.py -x</automated>
  </verify>
  <done>`yt-subdl preflight` is testable through Typer's runner, follows single-entry/subcommand shape, and maps required dependency failures to a non-zero exit code.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_preflight.py tests/test_cli_preflight.py -x` and `uv run ruff check src tests`. Confirm CLI code imports services, not infrastructure probe helpers directly.
</verification>

<success_criteria>
- User has a documented CLI command path for runtime preflight, satisfying RELY-05 and CLIX-01.
- Preflight checks cover yt-dlp, ffmpeg, ffprobe, and JS runtime candidates with actionable messages.
- CLI remains a thin adapter and uses the single root/subcommand structure locked by D-01.
</success_criteria>

<output>
After completion, create `.planning/phases/01-inspectable-intake-job-setup/01-02-SUMMARY.md`
</output>
