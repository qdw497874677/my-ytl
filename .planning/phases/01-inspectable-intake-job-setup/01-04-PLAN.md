---
phase: 01-inspectable-intake-job-setup
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/yt_subs/interfaces/cli.py
  - src/yt_subs/interfaces/rendering.py
  - tests/test_cli_inspect.py
  - README.md
autonomous: true
requirements:
  - INTK-01
  - INTK-02
  - INTK-03
  - INTK-04
  - EXPT-04
  - META-04
  - CLIX-01
  - CLIX-04
must_haves:
  truths:
    - "User can discover the CLI command set through help text with examples and next steps."
    - "User can run an explicit `inspect` subcommand to preview a video or playlist before download."
    - "User can set output directory/basic inspect options from the CLI without editing source code."
    - "Inspect CLI output includes expanded item identity, video ID, planned output path, and available subtitle languages."
  artifacts:
    - path: "src/yt_subs/interfaces/cli.py"
      provides: "Typer root app with preflight and inspect subcommands"
      contains: "def inspect"
    - path: "src/yt_subs/interfaces/rendering.py"
      provides: "Rich renderers for preflight/inspect reports kept separate from services"
      exports: ["render_inspect_result"]
    - path: "README.md"
      provides: "Documented Phase 1 CLI command examples and scope boundaries"
      contains: "yt-subdl preflight"
  key_links:
    - from: "src/yt_subs/interfaces/cli.py"
      to: "src/yt_subs/services/inspect.py"
      via: "inspect command calls service layer"
      pattern: "inspect_target"
    - from: "src/yt_subs/interfaces/rendering.py"
      to: "src/yt_subs/domain.models"
      via: "renderer consumes normalized domain objects, not yt-dlp dicts"
      pattern: "InspectItem|OutputIdentity"
    - from: "README.md"
      to: "src/yt_subs/interfaces/cli.py"
      via: "documented examples match actual subcommands"
      pattern: "yt-subdl inspect"
---

<objective>
Expose Phase 1 through the operation-oriented CLI and documentation.

Purpose: The user-facing value of Phase 1 is being able to preflight, inspect, and understand planned item/output identities before downloading. This plan wires the already-built services into the locked CLI shape.
Output: `yt-subdl inspect` and README examples that demonstrate preflight, URL intake, output-dir options, and inspect preview results.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-inspectable-intake-job-setup/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-inspectable-intake-job-setup/01-02-SUMMARY.md
@.planning/phases/01-inspectable-intake-job-setup/01-03-SUMMARY.md

<interfaces>
Use existing services:
```python
from yt_subs.services.preflight import run_preflight
from yt_subs.services.inspect import inspect_target
from yt_subs.domain.models import JobOptions
```

Locked decisions to implement:
- D-01: one root Typer app with subcommands.
- D-02: `inspect` is its own first-class subcommand, not `download --inspect` or `--dry-run`.
- D-03: keep temporary internal CLI name replaceable.
- D-04: output directory/basic job options live on business subcommands for now.
- D-05: help/docs are operation-oriented with examples and next steps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire inspect as a first-class Typer subcommand</name>
  <files>src/yt_subs/interfaces/cli.py, tests/test_cli_inspect.py</files>
  <behavior>
    - Test 1: `CliRunner().invoke(app, ["inspect", YOUTUBE_URL, "--output-dir", tmp_path])` calls `inspect_target` with the URL and JobOptions output directory.
    - Test 2: `inspect --help` shows examples and explains that inspect previews targets before download per D-05.
    - Test 3: invalid URL validation errors are shown as user-facing CLI errors with non-zero exit status.
  </behavior>
  <action>Extend the single Typer app from Plan 02 with an `inspect` command per D-02. Accept one target URL argument and business-level options such as `--output-dir` and any minimal inspect option needed for Phase 1 per D-04. Keep CLI code thin: parse options, construct `JobOptions`, call `inspect_target`, render results, map errors to exit codes. Do not add download, rerun, video, API, config-file, or JSON summary modes because those are outside/deferred for this phase.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_inspect.py -x</automated>
  </verify>
  <done>`inspect` is visible as an independent subcommand, accepts output directory from CLI, delegates to service code, and handles invalid input cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render inspect results without leaking yt-dlp internals</name>
  <files>src/yt_subs/interfaces/rendering.py, src/yt_subs/interfaces/cli.py, tests/test_cli_inspect.py</files>
  <behavior>
    - Test 1: CLI inspect output includes video ID and planned output bundle path.
    - Test 2: CLI inspect output includes available subtitle language codes and manual/automatic labels when present.
    - Test 3: playlist inspect output includes multiple item rows/sections from the fake service result.
  </behavior>
  <action>Create `rendering.py` for Rich rendering helpers so CLI command handlers stay small. Render normalized domain objects returned by the inspect service, not raw yt-dlp metadata. The output must make expanded items, subtitle language availability, stable identity, and planned paths visible to satisfy INTK-04, EXPT-04, and META-04. Keep the renderer human-readable and operation-oriented; do not implement a machine-readable output mode in this phase.</action>
  <verify>
    <automated>uv run pytest tests/test_cli_inspect.py -x</automated>
  </verify>
  <done>Inspect rendering exposes the Phase 1 success criteria and stays isolated from service/domain logic.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 1 CLI usage and boundaries</name>
  <files>README.md</files>
  <action>Create a concise README with setup, `uv run yt-subdl --help`, `uv run yt-subdl preflight`, and `uv run yt-subdl inspect <youtube-url> --output-dir <dir>` examples. Explain that Phase 1 inspects targets and planned outputs only; actual subtitle downloads/conversion/reruns arrive in later phases. Reference the temporary CLI name as internal/replaceable per D-03. Keep documentation operation-oriented per D-05 and do not promise features outside the roadmap.</action>
  <verify>
    <automated>uv run python -m typer yt_subs.interfaces.cli run --help</automated>
  </verify>
  <done>README gives the user enough commands to run preflight and inspect, documents output directory usage, and states current Phase 1 boundaries.</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_cli_preflight.py tests/test_cli_inspect.py -x`, `uv run ruff check src tests`, and `uv run python -m typer yt_subs.interfaces.cli run --help`. Confirm README examples match real subcommands and no direct yt-dlp import exists in CLI/rendering modules.
</verification>

<success_criteria>
- `preflight` and `inspect` are discoverable under one Typer root app.
- `inspect` is a standalone subcommand per D-02 and accepts output directory/basic options at the subcommand layer per D-04.
- User-facing inspect output shows expanded items, subtitle languages, stable video-ID identities, and deterministic planned output paths.
- README documents Phase 1 commands and clearly excludes actual download/conversion/rerun behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-inspectable-intake-job-setup/01-04-SUMMARY.md`
</output>
